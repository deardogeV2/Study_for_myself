spring:
  application:
    name: userService
  cloud:
    nacos:
      server-addr: 127.0.0.1:11086 # 注册中心地址
      config:
        file-extension: yaml # 文件后缀名
  config:
    activate:
      on-profile: dev # 环境配置